{
  "name": "ekocrop-jira-release-map",
  "private": true,
  "description": "Веб-карта релизов EkoCrop с загрузкой задач через Jira Automation",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test.mjs worker.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  },
  "engines": {
    "node": ">=20"
  }
}
